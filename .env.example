# Generate .env with scripts/configure.py; this is documentation, not a working password.
POSTGRES_PASSWORD=generate-a-random-password
MEMORY_PORT=8765
MEMORY_DATABASE_URL=postgresql://memory:generate-a-random-password@127.0.0.1:55432/memory

